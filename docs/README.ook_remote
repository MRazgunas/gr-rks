This is the ook_remote-write-a-block package meant as a guide to building
out-of-tree packages. To use the ook_remote blocks, the Python namespaces
is in 'ook_remote', which is imported as:

    import ook_remote

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(ook_remote)
